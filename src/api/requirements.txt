# Azure Functions for Python
azure-functions>=1.18.0,<2.0.0

# Application Insights for logging and monitoring
applicationinsights>=0.11.10
opencensus-ext-azure>=1.1.13
opencensus-ext-logging>=0.1.1

# Additional dependencies will be added as needed in subsequent tasks
# Preferring standard library dependencies where possible per requirements